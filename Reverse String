class Solution {
    char[] a;
    public void reverseString(char[] s) {
        this.a=s;
        swap(0,s.length-1);
    }
    void swap(int i,int j){
        if (i>=j){
            return ;
        }
        char temp=a[j];
        a[j]=a[i];
        a[i]=temp;
        swap(i+1, j-1);
    }
}
